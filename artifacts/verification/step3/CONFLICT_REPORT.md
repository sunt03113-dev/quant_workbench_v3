# STEP 3 冲突报告（fail-closed）— 需裁决

日期：2026-09-20 ｜ 运行：`app/golden_test.py`（Windows，qwb venv）
原始数据：`golden_test_results.json`（逐规则逐列统计与样本）

26 条 Golden 规则，**13 条完全 PASS，8 条仅 T+0 已知冲突，3 条 CONFLICT，1 条能力缺口**。
所有 CONFLICT 均已根因定位到具体行情 bar，无未解释差异。按协议：未裁决前不修改 Golden、不修改 Skill、不放宽 comparator。

---

## C1 ｜ T+0(低/高) 最高价取整方向 —— 影响 8 条规则全部 T+0 行（~1140 行）

- Golden：最高价**向上取整**（如 `-1%/9%(阳)`）
- 现行 Skill `generate_t_fields`：`fmt_tn` **向下取整**（注释自述"均变小"），得 `-1%/8%(阳)`
- STEP 2 已发现；最低价方向（`fmt_t0` 向上取整）双方一致。
- 判定：**Golden 与 Skill 真实冲突**（推测 Golden 生成后 Skill 改过取整规则）。

## C2 ｜ 涨停判定 `>=` vs 严格 `==` —— 影响 0824/0827A 共 7 行命中 + 1 个 (板) 标注

- 现行 Skill `compute_limit_flags`：`收盘==涨停价 且 最高==涨停价`（docstring 明确"严格 ==（不是 >=）"）
- Golden 生成口径：`>=`。证据（打包快照实测）：
  - `sz000539 1997-04-28` C=H=1981 分，昨收 1800 → 涨停价 1980（超出 1 分）→ Golden 0824 计入（D-1 涨停），我们排除
  - `sz000618 1997-09-10` C=H=1048 > 1047 → Golden 0827A 计入
  - `sz000019 1997-10-24` C=H=1142 > 1141 → Golden 排除（D-2 需非涨停），我们计入
  - `sz000612 2006-05-30`（停牌复牌后）C=H=905 > 902 → Golden T+2 标 `(板)`，我们不标
- 判定：**Golden 与 Skill 真实冲突**（Skill 演进：>= → ==）。

## C3 ｜ 0909A Golden 股票池不完整 —— 130 vs 10

- Golden 0909A 的 10 条命中**全部为沪市 600xxx**；我们的 130 条 = 同样 10 条（缺失 0）+ 120 条深市。
- Skill `0909A_backtest.collect_stock_files` 明确含沪深两市；规则文本【10cm】。
- 判定：**Golden 生成范围问题**（生成时未扫深市），非业务公式冲突。

## C4 ｜ 000995@2020-12-18 Golden 排除、快照数据下成立

- 在打包快照（=golden/vipdoc）上，该行满足 0824 全部条件（D-1=2020-12-17 涨停在 == 与 >= 下均成立）。
- 关键背景：sz000995 在 2020-12-16 重组复牌（+315%，前一根 bar 是 2019-04-25）。
- 判定：**Golden 生成时的 sz000995 行情数据与打包快照不一致**（推测生成后 TDX 补/修过数据），无法从快照复现。

## C5 ｜ rule.txt 输出段遗漏（Golden 有列、规则文本未写）

- 0818B：golden 有 `D-2涨幅`、`D-2振幅`，rule.txt 输出段未写
- 0908：golden 有 `D-4~D-1区间最大振幅`，rule.txt 未写（同构的 0908B/0908C 均写明）
- 判定：**冻结规则文本与 Golden 的一致性问题**，非 Skill/实现冲突。自然语言路径按 rule.txt 产出（当前行为：缺这两列）。

## C6 ｜ 0814 能力缺口（非冲突）

- 0814 为前向结构 + 可变间隔窗口（D+3~D+14，3~12 日），plan schema v1（向后日模式）无法表达。
- Skill 有完整实现（0814_backtest.py）。识别器已 fail-closed 并给出不支持提示。
- 处置建议：作为独立 plan kind（如 `forward_window`）列入后续版本，需你确认优先级。

---

## 已修复（无需裁决）

- 成交额百分比 float32/float64 边界（0918A 等 7 行 ±0.01%）：Skill 脚本内部为 float64，执行器已对齐，0918A 转 PASS。
- 300313 等时点差异：`data_end` 数据截断复现 Golden 生成视野后消除。

## 裁决请求

1. **C1**：T+0 最高价取整，以 Golden（向上）还是现行 Skill（向下）为准？
2. **C2**：涨停判定以 Golden（>=）还是现行 Skill（严格 ==）为准？
3. **C3**：0909A 以 Golden 现状（仅沪市）还是按规则文本/Skill（沪深两市）为准？
4. **C5**：是否在规则文本（rule.txt）中补写遗漏的输出列？
5. **C6**：0814 类前向窗口规则是否排入后续开发？

裁决前主链继续开发不受影响的部分：E2E 产品链路（STEP 4）以上述结论为准先行实现「现行 Skill 口径」，冲突裁决后一次切换。
