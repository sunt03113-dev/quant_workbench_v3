interval: 识别层新增「区间条件」语义 —— D-1/D-x 同组条件升级为 D-x~D-1 整段（kk 方案A）

现象：1S1A（strategy_c1790145091328_r1_v1）274 行，kk 参考 0923_backtest.xlsx 只有 131 行，
点名 603156@2026-05-18 错得离谱。复核确认 A(274) ⊃ B(131) 且共同 131 行字段零差异 ——
不是执行器算错数，是识别层语义缺口：工作台把 D-1 / D-x 当两个单日条件（只校验两端），
kk 参考脚本要求 [D-x, D-1] 整段每一天都满足「非涨停 + 非20日量最大 + 非20日价最高」。

改动
- app/executor.py：计划语言新增 strategy_plan.ranges；区间整段判定用每日满足掩码前缀和做 O(1)
  （_range_pref / _range_depths / _range_ok），固定日与变量日路径都接入；窗口不足 20 日
  fail-closed 判「不满足」（不可判定 ≠ 满足）。validate_rows 新增 _check_range_scalar 逐日独立
  重验 + RANGE_* 违例码（不复用向量化掩码，避免自证）。
- app/recognizer.py：显式写法（D-x~D-1：全部…）+ 自动升级（D-1 与 D-x 同组条件且输出含以
  D-x 为一端的区间指标）。回执 summary.notes 显式声明「已按区间语义翻译」，可审计非隐式。
  前向区间（D+3~D+14）不走区间分支，保持原 CAPABILITY_GAP 判定。
- app/ask.py：条件核验回答单列区间条件一行。
- app/recognizer.v5.baseline.py / executor.v4.baseline.py：变更前快照（用于重建端点式对照）。

验收（证据 artifacts/verification/1s1a_bug_0923/）
- 活体 HTTP：对运行中的 127.0.0.1:8000 发真实请求 → 识别回执含 ranges、回测 148 行；
  用 HEAD 版识别器重建的端点式计划回测 274 行（可复现修复前值）。
- 与 kk 参考逐键：148 = 131（交集 131 / 漏 0 / 数值真差异 0）+ 17 创业板；300 由 31→17
  （剔除 14 个假阳性、新增 0，全部落在 2020-08-24 前 10cm 时代）。
- 603156@2026-05-18 已消失（区间 2026-05-08~05-15 内含 05-13 封死涨停 + 05-12/05-14 创 20 日新高）。
- 6 个模型重跑落库、复核 0 违例；行数 1S1A 274→148 / 10cm_v6 622→323 /
  f120ccad35·676e25bb91·99b11761e0 55→323（旧计划为历史遗留错位翻译，一并纠正）/ 13b34ebf3a 377→214。
- golden 26 例同数据双跑与 HEAD 逐字段零回归；P9 win 重采 P0 26/26 判定一致（7849 行）。

注：strategy_13b34ebf3a_v1 同形态但无 kk 参考对照，待 kk 复核。
